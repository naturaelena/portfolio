This project realized via pandas, numpy, matplotlib, seaborn, sklearn
Before trainig ML model data has been analized with data visualisation, then data was splited with "train_test_split"
As a result we pompared MAE, RMSE, naive_mean, naive_median of Linreg and Decision tree models
